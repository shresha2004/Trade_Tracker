{
  "version": 2,
  "builds": [
    {
      "src": "App.js",  // path to the entry point file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "App.js"  // Ensure routing is correctly handled
    }
  ]
}
